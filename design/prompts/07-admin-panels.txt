Using the attached design system, continue the Spicy Meal staff admin dashboard. Desktop artboards at 1440x900, stacked vertically with a title label above each. English left-to-right. Reuse the same shell: a 220-wide left sidebar with the twelve navigation items and the active one as a filled purple pill, and a header row with title, description and right-aligned controls.

1. MENU MANAGEMENT. Header controls: "Download CSV template", "Import CSV" and a filled "+ New product". Main area split two-thirds / one-third. On the left a product table with columns Product, Category, Price, kcal, Active — four rows, each product showing its English name in bold with the Arabic name and modifier-group count beneath, prices right-aligned, and a toggle switch in the Active column with the last row switched off. On the right two stacked cards: Categories, listing Burgers · برجر with an "8 items" chip, Chicken · دجاج 6 items, Sides · المقبلات 5 items, Drinks · المشروبات 4 items; and Modifier groups, listing "Size · الحجم" with a red "Required · 1 of 1" chip and "Extras · إضافات" with a grey "Optional · up to 3" chip.

2. BRANCH MANAGEMENT. Header description "16 branches · channels, delivery zones and stock availability" and an "+ Add branch" button. Two-column row. Left card: the branch header "Olaya Branch · فرع العليا" with its address and phone and a green "Mapped to POS" chip; a two-by-two grid of stats — Delivery fee 10.00 SAR, Minimum order 25.00 SAR, Estimated delivery 35 min, Location "Set"; then three toggle rows — Delivery ON, Pickup ON, Delivery temporarily closed OFF. Right card: "Delivery area", a map panel showing faint road lines with a dashed purple polygon overlay and a red branch pin, and two buttons "Edit area" and "Clear". Below the two columns, a full-width "Branch × product availability" matrix: a header row of four branch names, then three product rows each with a toggle per branch, two of them switched off.

3. OPERATIONS ALERTS, rendered in DARK MODE using the dark tokens. Header description "Evaluated every 5 minutes · digest at 08:00 Asia/Riyadh", with a grey "In-dashboard only" chip and a Settings button. Two-column row. Left: three alert cards each with a coloured severity stripe. Red critical — "POS sync failing — Malqa", explaining four orders exceeded the retry deadline and the branch is unmapped, with the Arabic headline beneath and two buttons "Timeline" and "Open Order Integrity". Amber warning — "Delivery enabled without a zone" for Sulaimaniyah. Purple info — "Online payment disabled 3 h", noting cash orders continue and reach POS unpaid. Right: a daily digest card dated 2026-07-29, generated 08:00, AR + EN, with four figure rows — Orders completed 214, POS failures 4 in red, Refunds issued 1, Branches closed early 2 — and a footer note that external delivery is disabled by design so the digest is read here and nowhere else.

IMAGE RULE — REQUIRED FOR THE FILE TO SAVE. Do NOT insert image assets, image fills, or any external/remote image reference anywhere in this design. Every photograph must be drawn as a rounded rectangle filled with a warm appetising linear gradient (for food: #ffd9a3 into #f4a259 into #d1512f; vary the hue slightly between dishes) and, where it helps, a small muted caption. These are photo placeholders, which is standard mockup practice — production builds swap in real product shots at the same crop. Maps are likewise drawn with vector strokes, never an image tile.
