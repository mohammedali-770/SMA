node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
*.tsbuildinfo

# Sentry: local CLI auth must never be committed (auth token lives ONLY in
# EAS/CI encrypted secrets); bundle source maps are build artifacts.
.sentryclirc
*.js.map
*.bundle.map

# `npx expo start --web` rewrites apps/mobile/tsconfig.json#include (it strips
# .expo/types and expo-env.d.ts). That edit is generated, not authored, and must
# never reach a commit — see docs/DEV_VISUAL_REVIEW.md.
apps/mobile/expo-web.log
