import { supabase } from '../../lib/supabase';import type{DbProfile}from'../../types/db';export async function updateCustomerProfile(fullName:string,email:string):Promise<DbProfile>{const{data:{user}}=await supabase.auth.getUser();if(!user)throw new Error('You must be signed in to update your profile.');const{data,error}=await supabase.from('profiles').update({full_name:fullName,email:email||null}).eq('id',user.id).select('*').single();if(error)throw new Error(error.message);return data as DbProfile;}
