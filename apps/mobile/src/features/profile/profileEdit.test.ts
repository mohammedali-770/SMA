import{describe,expect,it}from'vitest';import{checkCustomerEmail,normalizeCustomerEmail}from'./profileEdit';describe('profile email',()=>{it('normalizes case and whitespace',()=>expect(normalizeCustomerEmail('  User@Example.COM ')).toBe('user@example.com'));it('allows an empty optional email',()=>expect(checkCustomerEmail('')).toEqual({valid:true,value:'',problem:null}));it('accepts a normal email',()=>expect(checkCustomerEmail('m@example.com').valid).toBe(true));it('rejects malformed email',()=>expect(checkCustomerEmail('not-an-email').problem).toBe('invalid'));});
